general {
  colors = true
  interval = 30
}

order += "tztime date"
order += "tztime time"

tztime date {
  locale = "es_DO.UTF-8"
  format = "%A %d/%m/%Y"
}

tztime time {
  locale = "en_US.UTF-8"
  format = "%I:%M %p"
}
