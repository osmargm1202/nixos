#!/usr/bin/env bash
set -euo pipefail

polybar-msg cmd quit >/dev/null 2>&1 || true
for _ in {1..30}; do
  pgrep -x polybar >/dev/null 2>&1 || break
  sleep 0.1
done
exec polybar --config="$HOME/.config/polybar/config.ini" modern
