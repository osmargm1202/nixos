#!/usr/bin/env bash
set -euo pipefail

if i3lock-fancy -t 'Ingrese su contraseña' "$@" -- scrot -z -o; then
  exit 0
fi

# Screenshot or ImageMagick failure must never leave session unlocked.
exec i3lock-color-fallback --color=2e3440ff --ignore-empty-password "$@"
