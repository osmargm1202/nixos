local _colors = {}
do
  local cfg = os.getenv("XDG_CONFIG_HOME") or (os.getenv("HOME") .. "/.config")
  local ok, v = pcall(dofile, cfg .. "/conky/orgm-colors.lua")
  if ok and type(v) == "table" then _colors = v end
end
local function col(k, fb) return _colors[k] or fb end

conky.config = {
    out_to_wayland = true,
    out_to_x = false,

    own_window_argb_value = 60,

    alignment = 'top_left',
    gap_x = 1454,
    gap_y = 810,

    minimum_width = 356,
    maximum_width = 356,
    minimum_height = 0,

    font = 'JetBrainsMono Nerd Font:size=13',

    default_color = col('text',      'cad3f5'),
    color1        = col('accent',    '8aadf4'),
    color2        = col('secondary', 'a5adcb'),

    update_interval = 2,
    double_buffer = true,
    draw_shades = true,
    draw_outline = true,
    draw_borders = false,
    border_width = 0,
    top_name_width = 16,
}

conky.text = [[
${voffset 10}${color1}${font JetBrainsMono Nerd Font:bold:size=12}${alignc}  Top Processes${font}${color}
${voffset 4}${color2}${font JetBrainsMono Nerd Font:size=10}   APP${alignr}CPU%   MEM%  ${font}${color}
${voffset 2}${color2}${font JetBrainsMono Nerd Font:size=12}   ${top name 1}${alignr}${top cpu 1}%   ${top mem 1}%
   ${top name 2}${alignr}${top cpu 2}%   ${top mem 2}%
   ${top name 3}${alignr}${top cpu 3}%   ${top mem 3}%  ${font}${color}
${voffset 8}
]]
