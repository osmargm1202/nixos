#!/bin/sh
set -eu

if [ "$#" -eq 0 ]; then
  set -- arch windows
fi

if command -v docker >/dev/null 2>&1; then
  docker start "$@" >/dev/null 2>&1 || true
elif command -v podman >/dev/null 2>&1; then
  podman start "$@" >/dev/null 2>&1 || true
fi
