# ORGM helper recovery sources

## Git recovery source

Primary source: dotfiles commit `b4ccf50^` for files deleted by commit `b4ccf50`.

## Broad backup search result

Broad backup search was run. No relevant dotfiles, Hyprland, Waybar, or ORGM helper backup directory was identified. Git commit `b4ccf50^` remains the authoritative recovery source.

